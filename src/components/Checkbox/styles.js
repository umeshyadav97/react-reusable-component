export const useStyles = () => {
  return {
    checkboxContainer: {
      display: "flex",
      flexDirection: "column",
      margin: "10"
    },
    innerContainer: {
      display: "flex",
      alignItems: "center"
    }
  }
}
